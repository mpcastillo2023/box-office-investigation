import React from "react";
type Props = {
    listMaxHeight?: React.CSSProperties["maxHeight"];
    shouldDisplayKey: boolean;
};
export default function List({ shouldDisplayKey, listMaxHeight }: Props): React.JSX.Element;
export {};

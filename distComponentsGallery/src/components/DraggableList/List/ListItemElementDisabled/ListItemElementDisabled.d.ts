import React from "react";
type Props = {
    children: React.ReactNode;
    listItemIndex: number;
    shouldDisplayKey: boolean;
};
export default function ListItemElementDisabled({ listItemIndex, children }: Props): React.JSX.Element;
export {};

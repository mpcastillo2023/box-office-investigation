import React from "react";
type Props = {
    /**
     * Custom styling classes for the spinner.
     * @param {customStyleClasses}
     */
    customClasses?: string;
};
export default function Spinner({ customClasses }: Props): React.JSX.Element;
export {};

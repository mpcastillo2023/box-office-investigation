export declare const getCurrentDateAndTime: (currentLocale?: string, date?: Date) => {
    currentDay: string;
    currentMonth: string;
    currentYear: string;
    currentTime: string;
    currentDate: string;
};
